{
  "name": "dental-care-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --env-file .env",
    "deploy": "wrangler deploy",
    "db:schema:local": "wrangler d1 execute dental-care-bookings --local --file=schema.sql",
    "db:schema:remote": "wrangler d1 execute dental-care-bookings --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  }
}
